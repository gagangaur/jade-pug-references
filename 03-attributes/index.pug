<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Attributes
    body
        //- input tyoe attributes
        input(type="password", name="pwd" data-js=`${ 5 > 2 ? "Ok" : "Not ok!"}`)
        
        //- div class attribute 
        -const myClasses = ["class1","class2","class3"]
        div.my-class(class=myClasses)
        
        //- div class attribute
        -const myStyles = {"color":"red","background-color":"blue"} 
        div(style=myStyles)

        //- image attributes
        -const myAttributes={"source":"mypic.png","alt":"my pic"}
        img&attributes(myAttributes)

        input(type="text" disabled)